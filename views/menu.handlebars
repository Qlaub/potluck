<div class="flex justify-between h-screen p-7">
  <h2 class="text-6xl">{{restaurantData.name}}</h2>
  <form class="flex flex-col basis-1/3 w-40 mt-10">
  {{#each restaurantData.dishes as |dish|}}
    <label class="basis-2 h-10 mx-1 my-1 font-bold">{{dish.name}}: ${{to_dollars dish.price_in_cents}}</label>
    <input class="h-10 mx-1 my-1 pl-2 rounded" type="number" min="0" max="10" placeholder="Quantity" id="{{dish.id}}">
  {{/each}}
  </form>
  <div class="basis-1/4 flex">
  <button id="orderBtn" class="basis-1/2 h-10 mt-40 bg-blue-500 hover:bg-blue-700 text-white text-xs font-bold py-2 px-4 rounded-full">Order Food</button>
  </div>
</div>

{{!-- <section>
  <h2>Pay It Forward</h2>
  <p>Your donation goes towards a free meal! After you donate to a restaurant, every person who orders food from that restaurant on our website receives up to $20 off their meal until your donation is met.</p>
  <form>
    <fieldset>
      <legend>Amount</legend>

      <div class="flex items-center mb-4">
        <input type="radio" id="100" name="amount" value="100" class="w-4 h-4 focus:ring-2 focus:ring-teal-300 ">
        <label for="100" class="block ml-2 text-sm font-medium text-gray-900">$100</label>
      </div>

      <div class="flex items-center mb-4">
        <input type="radio" id="50" name="amount" value="50" class="w-4 h-4 focus:ring-2 focus:ring-teal-300 ">
        <label for="50" class="block ml-2 text-sm font-medium text-gray-900">$50</label>
      </div>

      <div class="flex items-center mb-4">
        <input type="radio" id="25" name="amount" value="25" class="w-4 h-4 focus:ring-2 focus:ring-teal-300 ">
        <label for="25" class="block ml-2 text-sm font-medium text-gray-900">$25</label>
      </div>

      <div class="flex items-center mb-4">
        <input type="radio" id="other" name="amount" value="other" class="w-4 h-4 focus:ring-2 focus:ring-teal-300 ">
        <label for="other" class="block ml-2 text-sm font-medium text-gray-900">$ <input type="text"></label>
      </div>
    </fieldset>

    <div class="w-52 h-50">
      <select name="restaurant" id="restaurant" class="form-select block w-full px-3 py-1.5 text-base font-normal text-teal-700 bg-white bg-clip-padding border border-solid-gray-300 rounded transition ease-in-out m-0 focus:text-gray-700 focus:bg-white focus:border-blue-600 focus:outline-none" aria-label="Default select example">
        <option selected>Restaurant</option>
        <option value="chilis">Chili's</option>
      </select>
      <button type="submit" class="shadow bg-teal-900 hover:bg-teal-700 focus:shadow-outline focus:outline-none text-white font-bold py-2 px-4 rounded mt-2">Payment</button>
    </div>
  </form>
</section> --}}

<script src="/js/menu.js"></script>